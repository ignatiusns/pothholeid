import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "io.potholeid.demo",
  appName: "Photo Gallery Cap Ng",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
